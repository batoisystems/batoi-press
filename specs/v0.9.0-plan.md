# Batoi Press v0.9.0 Implementation Plan

## Tasks

- [x] Draft v0.9.0 implementation plan with release scope, tasks, and verification gates.
- [x] Add security baseline tests for installer lock configuration, private directory deny rules, upload allowlists, CSRF, rate limiting, roles, and author ownership.
- [x] Update security documentation to reflect implemented route-role and author-ownership enforcement.
- [x] Update user-facing release documentation and version metadata for `0.9.0`.
- [x] Run PHP syntax checks, smoke tests, update runner tests, static export tests, role access tests, post ownership tests, security baseline tests, release build, manifest generation, and artifact verification.
- [x] Commit v0.9.0 planning, implementation, docs, and release metadata to this repository.
- [x] Update `/Users/ashwinirath/Sites/localhost/exp/testsite` with the current repo files for local review.
- [x] Publish generated `dist/latest.json` and `dist/batoi-press-0.9.0.zip` into neighboring `batoi-www/public_html/pub/press/`.

## Goal

Create a reproducible pre-1.0 security baseline so core hardening controls can be verified locally before the stable-readiness pass.

## Release Theme

Security baseline verification.

## Scope

### 1. Security Baseline Test

Status: Pending

Tasks:

- [x] Verify installer lock configuration points to `radpress/config/installed.lock`.
- [x] Verify private runtime directories include Apache deny rules.
- [x] Verify public rewrite rules keep only expected PHP entrypoints executable.
- [x] Verify upload allowlist blocks executable extensions and normalizes filenames.
- [x] Verify CSRF token validation accepts the current token and rejects invalid tokens.
- [x] Verify rate limiting blocks after configured attempts.
- [x] Verify role-route and author-post ownership helpers remain enforced.

Acceptance:

- Security controls are checked by a single local test command.
- The test fails when critical baseline controls are missing or weakened.

### 2. Documentation Alignment

Status: Pending

Tasks:

- [x] Replace outdated security wording that says role enforcement is future work.
- [x] Document route-role enforcement and author-owned post governance.
- [x] Add `radpress/docs/releases/v0.9.0.md`.
- [x] Update `radpress/docs/release-management.md` current release track.
- [x] Update `radpress/config/update.json` to `0.9.0`.

Acceptance:

- Security docs match implemented behavior.
- Release notes explain exactly what v0.9.0 verifies.

### 3. Release Publication

Status: Pending

Tasks:

- [x] Build release package and manifest after verification.
- [x] Verify release package and public manifest with `tools/verify-release-artifacts.php`.
- [x] Copy release files to the neighboring `batoi-www` publication directory.
- [x] Commit repository changes after verification.

Acceptance:

- Release artifacts and version metadata agree.
- Testsite and public release files are updated from the verified checkout.

## Out of Scope for v0.9.0

- Browser-driven penetration testing.
- New authentication providers.
- Mandatory signed release packages.
- Breaking changes to content files, theme templates, public routes, user storage, or update package structure.

## Verification Plan

- Run `php -l` on changed PHP files.
- Run `php radpress/tests/smoke.php`.
- Run `php radpress/tests/update_runner.php`.
- Run `php radpress/tests/static_export.php`.
- Run `php radpress/tests/role_access.php`.
- Run `php radpress/tests/post_ownership.php`.
- Run `php radpress/tests/security_baseline.php`.
- Run `php tools/build-release.php`.
- Run `php tools/generate-release-manifest.php`.
- Run `php tools/verify-release-artifacts.php`.
- Verify `dist/latest.json` and `dist/batoi-press-0.9.0.zip` before copying to `batoi-www`.

## Recommended First Implementation Slice

Start with `radpress/tests/security_baseline.php` because it converts several 1.0 checklist items into repeatable evidence without changing runtime behavior.

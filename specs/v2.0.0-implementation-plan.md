# Batoi Press v2.0.0 Implementation Plan

## Tasks

- [x] Inventory the current `1.8.0` product, runtime, administration, security, AI scaffolding, update, test, and publication surfaces.
- [x] Define the `2.0.0` product priorities and compatibility boundaries.
- [x] Validate the remote MCP direction against current OpenAI, Claude, and MCP authorization guidance.
- [x] Define the professional navigation scope for structured menus, dropdowns, child menus, mega menus, and default-theme delivery.
- [x] Add the first machine-access security foundation: hashed access tokens, explicit scopes, expiry, revocation, safe metadata, and regression coverage.
- [x] Add a shared application read service for governed site, page, post, and menu operations used by API and MCP.
- [ ] Add shared application mutation services for page, post, media, menu, settings, and taxonomy operations, then route Admin/API/MCP writes through them.
- [ ] Add atomic writes, resource revisions, optimistic concurrency, idempotency keys, and write-conflict responses.
- [x] Route page and post Admin/API/MCP mutations through one bounded service with atomic file replacement, revision preconditions, idempotent create/publish operations, snapshots, and safe audit summaries.
- [x] Upgrade the primary-menu storage from URL-linked rows to stable item IDs, typed items, explicit hierarchy, revisions, snapshots, and backward-compatible migration.
- [x] Generalize the structured menu repository and editor from the primary menu to theme-declared menu locations, beginning with `primary` and `footer`.
- [x] Build a professional tree-based menu editor with drag/drop, button/keyboard reordering, item settings, hierarchy validation, and a live structural preview.
- [x] Add accessible dropdown, nested-child, and mega-menu rendering to the default theme with desktop and mobile interaction behavior.
- [x] Add primary-menu migration, repository, controller, hierarchy, validation, and theme-hook regression coverage.
- [ ] Add dedicated static-export, full keyboard-flow, viewport-edge, and automated accessibility coverage for structured menus.
- [x] Add a read-only versioned JSON API with bearer scopes, consistent errors, pagination, filtering, revisions/ETags, rate limits, and audit correlation IDs.
- [x] Add owner-only machine-access administration with one-time token display, rollout-safe scope selection, mandatory expiry, revocation, password re-authentication, throttling, and audit events.
- [x] Require MFA re-authentication for connection changes after the MFA enrollment and recovery flow is available.
- [x] Add the OAuth resource-server layer for an established external identity provider: protected-resource metadata, discoverable challenges, JWT signature/issuer/audience/time/scope validation, bounded JWKS retrieval/cache, and per-tool security schemes.
- [ ] Configure and validate an established OAuth 2.1 authorization server with Authorization Code + PKCE, CIMD or DCR/pre-registration, exact redirects, refresh/revocation policy, and authorization-server metadata.
- [x] Add a stateless Streamable HTTP MCP endpoint with protocol negotiation, transport/Origin validation, scoped tools/resources, and an explicit no-SSE GET response.
- [x] Add safe read tools and resources before enabling draft writes, updates, publishing, or media mutations.
- [x] Add scoped page/post draft-create, revision-checked draft-update, and explicit publish API/MCP operations after the write-safety gates passed.
- [ ] Complete OAuth-backed remote connector setup and interoperability testing for Claude, ChatGPT, Codex, and other compatible clients.
- [ ] Add editorial workflow, scheduled publishing, taxonomy, reusable content blocks, and content health features.
- [x] Add compatible editorial states, reviewer assignment/notes/history, scheduled publish/unpublish visibility, and shared category/tag counts for Admin/API/MCP.
- [ ] Expand Batoi AIF into the integrated, opt-in intelligence layer with guarded provider adapters and assisted-authoring workflows.
- [x] Integrate Batoi AIF into page/post editors with an allowlisted context boundary, offline local provider, content-health/SEO/summary/tag/outline actions, explicit apply/copy review, throttling, safe audit metadata, and no publish authority.
- [ ] Add MFA, session lifetime controls, security headers, signed update packages, secret handling, and security diagnostics.
- [x] Add TOTP MFA with hashed one-time recovery codes, encrypted secret storage, MFA login and connection step-up, idle/absolute session expiry, report-only CSP/HSTS/browser headers, and upload MIME/signature validation.
- [ ] Complete migration, compatibility, threat-model, operator, MCP setup, API, and release documentation.
- [ ] Run the complete PHP and release verification matrix; add protocol, authorization, concurrency, and security tests.
- [ ] Increment release metadata to `2.0.0`, build and verify `dist/batoi-press-2.0.0.zip` and `dist/latest.json`.
- [ ] Commit verified `2.0.0` work to this repository in coherent implementation slices.
- [ ] Synchronize the verified checkout into `/Users/ashwinirath/Sites/localhost/exp/testsite` and complete browser review.
- [ ] Copy only verified release artifacts into `/Users/ashwinirath/Sites/localhost/gitrepo/batoi-www/public_html/pub/press/` and verify checksums there.
- [ ] Commit the publication changes in the neighboring `batoi-www` repository separately.

## Goal

Make Batoi Press a secure, automation-ready publishing system while preserving its principal advantage: a portable PHP and flat-file deployment that works on conventional hosting without a mandatory database, Node.js, Docker, Git, or shell access.

Version 2.0 should let a person safely ask Claude, ChatGPT, Codex, or another MCP client to inspect and update a site. Machine actions must go through the same validation, ownership, versioning, and audit boundaries as admin actions. Generated content must never bypass review or publish merely because it originated from an AI client.

## Current `1.8.0` Baseline

The current release already provides the following useful foundation.

### Runtime and publishing

- PHP 8.1+ runtime with JSON configuration and database-free page/post storage.
- Public pages, hierarchical pages, posts, blog, archive, sitemap, RSS, media, typed assets, and theme assets.
- Sanitized HTML content, SEO metadata, publishing dates, post ownership, page templates, and version snapshots.
- Responsive default Batoi Versatile theme with company, editorial, campaign, contact, and ecommerce presentation layouts.
- Static ZIP export with theme, application, library, branding, typed asset, and legacy media coverage.

### Administration and governance

- Authenticated B2B admin console for pages, posts, media, menus, widgets, settings, themes, users, audit, cache, export, updates, and AIF status.
- Owner, admin, editor, author, and viewer roles with route and post-ownership enforcement.
- Search and filtering across principal operational lists.
- Rich HTML/source editing, focus mode, media guidance, theme preview, guarded theme editing, branding, frontend-library management, and user lifecycle controls.
- Audit logging with search, filters, export, cleanup, outcomes, and blocked-access events.

### Operations and security

- Installer lock, password hashing, file-backed sessions, CSRF protection, login rate limiting, upload allowlists, generated upload names, and private runtime directories.
- Staged updates with checksum verification, path validation, backups, maintenance mode, health checks, automatic rollback, and manual restore.
- Release build, manifest generation, artifact verification, testsite synchronization practice, and stable public manifest publication.
- Disabled-by-default AIF facade with feature flags, explicit POST actions, trust-boundary copy, and audit hooks; no active remote provider exists yet.

## Product Priorities

### P0: Safe automation contract

Build one internal application-service layer and require the Admin UI, JSON API, and MCP tools to use it. Controllers must not be duplicated as an API. The service layer owns validation, sanitization, authorization decisions, revisions, audit metadata, and file mutations.

The initial automation surface should support:

- Site identity and capability discovery.
- Listing and reading pages and posts.
- Creating drafts.
- Updating a draft with a required expected revision.
- Requesting publication as a distinct, more privileged action.
- Reading media metadata and obtaining stable asset URLs.
- Reading menus, taxonomies, templates, and content-health findings.

Deletion, theme source editing, update installation, user administration, token administration, raw filesystem access, arbitrary URLs, arbitrary PHP, and arbitrary HTML execution are not MCP tools in the initial 2.0 release.

### P0: Professional navigation and menu management

Menu management is a core publishing workflow in 2.0, not a basic label/URL settings form. The Admin Console must support calm, high-density management of simple links, dropdown menus, multi-level child menus, and responsive mega menus while preserving the existing flat-file deployment model.

#### Menu data contract

- Introduce a schema-versioned menu document while continuing to read existing `label`, `url`, and URL-based `parent` records.
- Give every menu and menu item a stable generated ID. Hierarchy references use `parent_id`; changing a URL must not break child relationships.
- Support named menu locations beginning with `primary` and `footer`, with a theme-declared location contract for future themes.
- Support item types for published page, post, taxonomy/archive, custom link, non-link heading/group, and optional separator.
- Store common fields consistently: label, destination reference or URL, parent ID, order, enabled state, and item type.
- Store presentation settings only where relevant: normal dropdown or mega menu, mega-menu column/group assignment, concise description, and optional locally managed icon/image reference.
- Use explicit limits: bounded item count, bounded depth, bounded mega-menu columns, bounded labels/descriptions, and no arbitrary HTML, CSS, JavaScript, PHP, or remote embed fields.
- Validate internal references, safe URL schemes, duplicate IDs, missing parents, cycles, excessive depth, invalid mega-menu grouping, and unavailable destinations before publication.
- Preserve unknown compatible fields during migrations where practical and snapshot the previous menu document before every mutation.

Proposed compatible shape:

```json
{
  "schema_version": 2,
  "id": "menu_primary",
  "name": "Primary navigation",
  "location": "primary",
  "items": [
    {
      "id": "mi_company",
      "type": "page",
      "label": "Company",
      "url": "/about",
      "parent_id": null,
      "presentation": "dropdown",
      "enabled": true
    }
  ]
}
```

#### Admin experience

- Replace repeated blank rows and manually typed Parent URLs with a proper hierarchical tree.
- Provide an item library for published pages, posts, archives, and custom links; adding an item should not require remembering a URL.
- Support add, edit, duplicate, enable/disable, expand/collapse, indent/outdent, move up/down, and remove actions from a consistent item row and detail drawer.
- Support pointer drag/drop for speed and explicit keyboard controls for accessibility and precision. Keyboard reordering must not depend on drag/drop.
- Show item type, destination, state, depth, child count, and dropdown/mega presentation at a glance.
- Give top-level items a presentation choice: standard link, dropdown, or mega menu. Show mega-menu column/group controls only when mega presentation is selected.
- Provide a structural preview that shows hierarchy, columns, group headings, disabled/broken items, and the selected menu location without pretending to be a full theme preview.
- Validate before saving and return row-specific errors without discarding the submitted structure.
- Warn about unpublished, missing, circular, excessively deep, empty dropdown, and empty mega-menu destinations.
- Preserve the legacy line editor only as an import/migration tool, not as the primary editing interface.
- Record semantic audit events with menu ID, location, actor, correlation ID, previous revision, resulting revision, and safe change summary.

#### Default-theme navigation

- Render primary navigation from the structured menu contract while retaining a safe fallback for legacy menus.
- Support ordinary top-level links, dropdowns, nested child menus, non-link group headings, and multi-column mega menus in the bundled default theme.
- Keep parent navigation and submenu disclosure as separate accessible controls when the parent also has a destination.
- Maintain `aria-expanded`, `aria-controls`, current-page, and current-ancestor states.
- Support keyboard navigation, Enter/Space disclosure, Escape to close and restore focus, outside-click dismissal, and predictable focus order.
- Do not make hover the only way to open navigation. Hover and `focus-within` may enhance pointer use while click/keyboard behavior remains authoritative.
- Prevent dropdowns from rendering outside the viewport; align edge menus safely and constrain mega-menu width to the navigation container/viewport.
- Use mobile accordion behavior with clear disclosure buttons, nested indentation, and no hover dependency.
- Use progressive enhancement: destinations remain usable when JavaScript is unavailable, and markup remains semantic.
- Mark the active item and its ancestors without automatically opening every ancestor on initial load.
- Keep styling consistent with the Batoi design baseline: compact controls, restrained borders/shadows, white surfaces, Batoi Blue interaction states, and no decorative gradients.
- Include structured menus in static exports without introducing a client-side rendering dependency.

#### Menu authorization and automation

- Owners and administrators may manage all menu locations. Editors may manage content-navigation locations only when explicitly allowed by policy.
- Future API/MCP operations use stable IDs and revisions, not array indexes or URL parent references.
- Add read operations `menu_list` and `menu_get` under `site:read`.
- Add a later `menu_update` operation under `site:write` with expected revision, idempotency key, bounded complete-tree input, validation-only/dry-run support, and a returned structural diff.
- Do not expose raw theme markup, arbitrary scripts, user management, or update operations through menu tools.

### P0: Security and trust

- Machine tokens are random, shown once, stored only as password hashes, scoped, expiring, revocable, and attributable to an issuing user.
- OAuth uses Authorization Code with PKCE and current MCP discovery metadata. Do not build token validation or OAuth protocol logic ad hoc; use an audited, maintained implementation suitable for bundled PHP distribution or a documented Batoi identity broker.
- Separate scopes for reading, drafting/updating, publishing, media mutation, site configuration, and audit access.
- Require step-up/MFA re-authentication for token creation, OAuth consent, publish grants, user administration, update/rollback, and other sensitive owner actions.
- Require optimistic concurrency for every machine write so an agent cannot silently overwrite a newer human or agent revision.
- Require idempotency keys for creates, publishes, uploads, and other retry-sensitive operations.
- Keep read and write tools separate. Tool names, descriptions, annotations, and responses must make side effects unambiguous so clients can apply approval policies.
- Record actor, client, token grant, tool/API operation, correlation ID, target revision, resulting revision, outcome, and safe field-level change summary. Never log credentials or full sensitive prompts.
- Default all AI-assisted output to suggestion or draft state. Publishing requires an explicit publish call and publish scope.

### P1: Editorial and content capabilities

- Workflow states: draft, in review, approved, scheduled, published, archived.
- Review assignments, comments, approval history, scheduled publish/unpublish, and preview links with bounded expiry.
- Categories, tags, canonical URLs, redirects, Open Graph/social metadata, structured data, and content relationships.
- Reusable content blocks/patterns without making stored content dependent on a JavaScript renderer.
- Content-health checks for broken links, missing alt text, missing metadata, stale content, orphan pages, and redirect conflicts.
- Bulk operations with preview/dry-run, bounded item counts, and one audit correlation ID.
- Import/export for Markdown and structured JSON while retaining sanitized HTML as the stable rendering contract.

### P1: Integrated Batoi AIF

Batoi AIF is a first-class integrated feature of Batoi Press. It owns assisted-authoring policy, prompts, context preparation, provider routing, usage controls, audit metadata, and the review boundary. OpenAI, Anthropic, Batoi-hosted inference, or another service may be configured behind AIF, but those services do not replace the Batoi AIF product surface or contract.

Keep inbound MCP automation separate from outbound AIF model-provider calls. MCP lets an authorized external client operate Press; AIF provides native intelligence inside Press. A site can support MCP without sending content to an AIF provider, and AIF can assist an administrator without exposing the site through MCP.

Opt-in assisted features should include:

- Outline and first-draft suggestions.
- SEO title/description and social-copy suggestions.
- Summaries, excerpt generation, tags, internal-link suggestions, and content refresh suggestions.
- Accessible alt-text suggestions with explicit human review.
- Translation drafts with locale and glossary context.
- Content transformation between approved templates or tone profiles.

Batoi AIF provider adapters may support Batoi-hosted intelligence, OpenAI, Anthropic, or another configured service behind a stable AIF contract. Credentials stay outside public files and content. Administrators control which fields may leave the installation, retention expectations, quotas, model/provider choice, and feature flags. Prompt injection from site content is treated as untrusted input; content cannot grant tools, scopes, or publication permission.

### P1: Security hardening

- TOTP and WebAuthn/passkey MFA with recovery codes and enforced owner protection.
- Idle and absolute session lifetimes, session inventory/revocation, password rehashing, login notifications, and optional trusted-device controls.
- Content Security Policy rollout with nonces/report-only validation first, plus HSTS on HTTPS, Referrer-Policy, Permissions-Policy, frame restrictions, and consistent cache controls.
- MIME-signature validation for every upload, robust SVG sanitization or raster-only policy, archive budgets, image decompression limits, and optional malware-scanner hook.
- Encrypted secret storage using a host/environment key; secrets are never written to tracked configuration, exports, backups without encryption, logs, or release packages.
- Signed update manifests/packages using an offline release key, with key rotation and checksum retained as an additional integrity check.
- Backup encryption option, restore drills, release SBOM, dependency/license inventory, security contact, vulnerability disclosure, and supported-version policy.
- Rate limits by IP, user, token, client, and operation; bounded response sizes; pagination ceilings; request-body limits; and optional IP allowlists.
- SSRF protections for every outbound fetch: HTTPS allowlists where possible, DNS/IP validation, redirect limits, timeouts, size limits, and private-address denial.

### P2: Operations and ecosystem

- Webhooks for publish/update/media events with signed payloads, delivery history, retries, replay protection, and secret rotation.
- Health/status endpoint that reveals no secrets and separates public liveness from authenticated diagnostics.
- Backup scheduling and retention policies compatible with cron when available and manual operation when it is not.
- Optional search index generated from flat files; index data is disposable and rebuildable.
- Optional Batoi Workspace fleet visibility, managed policy, centralized OAuth identity, security posture, and assisted upgrades without making Workspace mandatory for a standalone site.
- Extension hooks with declared capabilities and isolation boundaries; no implicit execution of uploaded PHP extensions.

## Automation Architecture

```text
Admin UI -----\
JSON API ------> Application services -> validators/policies -> repositories -> flat files
Remote MCP ---/          |                       |                 |
                         +---- audit log --------+---- revisions --+
```

### Versioned JSON API

Use `/api/v2/...` with JSON request and response bodies, bearer authorization, stable error codes, pagination cursors, result limits, correlation IDs, and `ETag`/revision preconditions. Prefer resource-shaped endpoints over controller-shaped RPC. CORS remains disabled unless explicitly configured with exact origins.

### Remote MCP server

Expose a single configurable Streamable HTTP endpoint, proposed as `/mcp`, with protocol-version negotiation and standard initialize, tools, resources, and logging behavior. The server must be reachable over HTTPS for cloud-hosted Claude/ChatGPT connections. Private installations should use a supported secure tunnel or an optional Batoi-hosted gateway rather than disabling authentication or exposing an admin session.

Initial tool families:

- `site_get`
- `page_list`, `page_get`, `page_create_draft`, `page_update_draft`, `page_publish`
- `post_list`, `post_get`, `post_create_draft`, `post_update_draft`, `post_publish`
- `media_list`, `media_get`
- `menu_list`, `menu_get`
- `taxonomy_list`
- `content_health_check`

Initial resources may expose the site profile, content schemas, allowed templates, taxonomies, and individual read-only content records. Resource and tool outputs must be bounded and must not expose filesystem paths, password hashes, tokens, secrets, private logs, or unpublished content without the matching scope.

### Authentication modes

1. Hashed personal/machine access tokens are the first developer and test foundation. They are suitable for scripts, controlled API clients, and server-to-server calls that can send a bearer token.
2. OAuth 2.1 with PKCE is the required end-user remote MCP experience for Claude and ChatGPT. It uses per-user consent, exact redirect validation, short-lived access tokens, rotating refresh tokens, resource/audience binding, scope enforcement, revocation, and no token passthrough.
3. A Batoi Workspace broker may provide hosted identity and private-site tunneling, but a standalone public installation remains possible.

## Phased Implementation

### Phase 0: Contract and threat model

Deliverables:

- This plan, protocol decision records, data-flow diagram, threat model, and compatibility matrix.
- Explicit stable and experimental tool/API declarations.
- Migration fixtures covering representative `1.8.0` content and customized config.

Exit gate:

- No externally reachable machine-write route exists until authorization, revisions, idempotency, audit attribution, rate limiting, and tests are ready together.

### Phase 1: Machine-access and write-safety foundation

Deliverables:

- Hashed, scoped, expiring, revocable access-token repository.
- Owner-only token administration and secure one-time display.
- Application services and policy objects shared by admin/API/MCP.
- Schema-versioned menu repository with stable IDs, hierarchy validation, migration, revisions, and snapshots.
- Atomic JSON/HTML commits, per-resource revisions, preconditions, locks, and idempotency records.
- Audit correlation and safe change summaries.

Acceptance:

- Plain tokens cannot be recovered from storage or listing responses.
- Invalid, expired, revoked, incorrectly scoped, or malformed tokens fail closed.
- Concurrent writes produce a conflict and preserve both the current file and its history.
- Retried creates/publishes with the same idempotency key do not duplicate effects.

### Phase 2: Professional navigation

Deliverables:

- Backward-compatible migration from URL-parent rows to schema-versioned menus with stable item IDs.
- Primary/footer menu locations and theme-declared location support.
- Professional tree editor with item library, settings drawer, pointer and keyboard reordering, hierarchy validation, revision history, and structural preview.
- Default-theme dropdown, nested-child, group-heading, mega-menu, current-ancestor, viewport-edge, and mobile-accordion behavior.
- Static-export and browser regression coverage for simple, nested, and mega menus.

Acceptance:

- Existing flat menus retain their visible order, labels, URLs, and public behavior after migration.
- Changing a parent URL does not detach its children.
- Admin users can build and revise navigation without manually entering parent URLs.
- Default-theme navigation works with pointer, keyboard, touch, narrow screens, zoom, and reduced-motion preferences.
- Invalid, cyclic, excessive-depth, unsafe-URL, broken-reference, and malformed mega-menu structures fail safely with actionable row-level feedback.

### Phase 3: Read-only API and MCP

Deliverables:

- `/api/v2` discovery and read endpoints.
- `/mcp` Streamable HTTP endpoint with read-only tools/resources.
- Token authentication, per-operation rate limits, request limits, structured errors, and audit events.
- Setup documentation and tested configurations for compatible developer clients.

Acceptance:

- Claude/ChatGPT-compatible clients can discover site capabilities and read only the content allowed by granted scopes.
- Unauthenticated, incorrectly scoped, cross-site, oversized, traversal, and protocol-invalid requests fail safely.

### Phase 4: Draft writes and OAuth

Deliverables:

- OAuth 2.1/PKCE discovery, consent, token, refresh, revocation, and audience validation through the selected audited implementation.
- Draft-create and revision-checked update tools/API endpoints.
- Separate human-readable approval information for mutating tools.
- Preview/diff response before or after mutation as appropriate.

Acceptance:

- Claude and ChatGPT can connect through a normal OAuth consent flow and create/update drafts without receiving admin credentials.
- A draft scope cannot publish, change users, change secrets, install updates, edit PHP, or delete content.

### Phase 5: Publishing and editorial workflow

Deliverables:

- Review, approval, scheduling, taxonomy, redirect, and explicit publish operations.
- Publish scope and optional policy requiring in-product human approval even when a client holds the scope.
- Webhooks with signing and replay protection.
- Revision-checked `menu_update` API/MCP operation after the Admin workflow and read operations are stable.

Acceptance:

- Publication is attributable, revision-safe, reversible through history, and independently governable from drafting.

### Phase 6: Assisted authoring and security completion

Deliverables:

- Integrated Batoi AIF services, guarded provider adapters, and assisted-authoring actions.
- MFA/session/security-header/upload/secret/update-signature hardening.
- Security diagnostics, operator runbooks, privacy controls, quotas, and retention policy.

Acceptance:

- AI provider calls remain disabled by default and disclose exactly what content is sent.
- Generated suggestions cannot self-authorize tools or bypass editorial state.
- Security verification and release-signature checks pass in supported hosting modes.

### Phase 7: Release candidate, migration, and publication

Deliverables:

- `2.0.0-rc` migration rehearsal against a copy of the testsite.
- Full regression, protocol, security, browser, update-from-1.8.0, rollback, static-export, and artifact tests.
- Final documentation, release notes, verified artifacts, testsite synchronization, and `batoi-www` publication.

Acceptance:

- Existing content, themes, URLs, installer lock, and supported customized settings survive upgrade and rollback.
- Publication files exactly match locally verified SHA-256 values.

## Compatibility Commitments

- Preserve the page/post `meta.json` plus `body.html` contract; add fields compatibly.
- Preserve existing public routes, `/media/`, `/assets/`, `/theme-assets/`, installer lock, theme contracts, and static-export behavior.
- Preserve existing `radpress/content/menus/main.json` label/URL records through read compatibility and a reversible migration to stable menu/item IDs.
- Keep PHP 8.1 as the target unless a documented dependency and hosting review justifies a change.
- Keep a database optional. Disposable indexes and queues may use SQLite only as an optional acceleration layer with flat-file fallback.
- Keep AIF and remote integrations disabled until an administrator explicitly configures them.
- Do not overwrite customized site, security, user, content, provider-secret, token, or OAuth runtime data during updates.

## Verification Matrix

- Run `php -l` on every changed PHP file.
- Run every existing test in `radpress/tests/` plus new machine-access, API, MCP protocol, OAuth, revision-conflict, idempotency, and security-header suites.
- Test malformed JSON, duplicate keys where relevant, oversized bodies, invalid encodings, traversal, symlinks, archive bombs, SSRF redirects, token leakage, prompt injection, scope escalation, replay, and race conditions.
- Test menu ID stability, URL changes with children, missing parents, cycles, depth/column limits, disabled items, broken destinations, legacy migration, revision conflicts, and idempotent updates.
- Test root and subdirectory installations on supported Apache/PHP configurations.
- Browser-review desktop and 390-pixel mobile admin flows in the synchronized testsite.
- Browser-review pointer, keyboard, focus, Escape, outside-click, current-ancestor, viewport-edge, touch accordion, nested dropdown, and mega-menu behavior in the default theme.
- Test upgrade from a representative `1.8.0` installation, failed migration rollback, update rollback, backup restore, and static export.
- Run `php tools/build-release.php`, `php tools/generate-release-manifest.php`, and `php tools/verify-release-artifacts.php` only after source verification.
- After copying to `batoi-www`, compare manifest and ZIP hashes against the verified `dist/` files before committing that repository.

## Release and Repository Workflow

Use coherent commits rather than one opaque release commit. Suggested sequence:

1. Plan, threat model, and compatibility fixtures.
2. Machine access, application services, revisions, and tests.
3. Read-only API/MCP and protocol tests.
4. OAuth and draft-write integration.
5. Workflow, AI assistance, and hardening.
6. Release metadata, docs, verified artifacts, and publication.

For each slice:

- Keep the primary repository clean except for the intended slice.
- Run proportional tests before committing.
- Synchronize runtime/UI changes to testsite only after the source tests pass.
- Perform browser review for every user-facing change.
- Do not publish interim `latest.json` or a stable `2.0.0` ZIP.

For the final release:

- Build and verify artifacts from the committed source tree.
- Copy with `php specs/local/copy-release-to-batoi-www.php --target /Users/ashwinirath/Sites/localhost/gitrepo/batoi-www`.
- Verify destination file sizes and SHA-256 hashes.
- Inspect both repositories' diffs.
- Commit the Batoi Press source/release state and the `batoi-www` publication state in their respective repositories.

## External Standards Notes

The implementation should follow the current MCP authorization specification: HTTP authorization is based on OAuth 2.1 conventions, protected-resource and authorization-server metadata, PKCE, HTTPS, exact redirect validation, short-lived tokens, and resource/audience binding. Tokens intended for another resource must not be accepted or passed through.

Current OpenAI guidance supports remote MCP servers over Streamable HTTP or HTTP/SSE, supports bearer/OAuth authorization, tool filtering, and per-tool approval policies, and warns that remote MCP servers are a material data-exfiltration trust boundary.

Current Claude guidance supports custom remote MCP connectors reached from Anthropic's cloud infrastructure and emphasizes OAuth permissions, trusted servers, prompt-injection awareness, disabling unnecessary tools, and careful approval of write actions.

These external client capabilities can evolve during implementation. Re-check the official OpenAI, Claude, and MCP documentation when Phase 2 and Phase 3 begin instead of pinning client-specific behavior from this planning date.
